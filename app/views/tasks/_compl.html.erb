<%- model_class = Task -%>

<div class="page-header ">
  <h1 class="col-md-offset-4" >Completed tasks</h1>
</div>
<div class="container">
    <div class="row col-md-7 col-md-offset-2 custyle">
    <table class="table table-striped custab">
    <thead>
        <tr>
            <th>Check</th>
            <th><%=sortable "name"%></th>
            <th>Duedate</th>
            <th>Priority</th>
            <th class="text-center">Action</th>
        </tr>
    </thead>
          <%  @tasks_completed.each do |task| %>
            <tr>
                <td><%= check_box_tag 'tasks_id[]', task.id, false %></td>
                <td><%= task.name %></td>
                <td ><%= task.duedate %></td>
                <td>Priority</td>
                <td>
                    <%= link_to 'Uncomplete' , active_again_task_path(task), :class => 'btn btn-danger btn-xs' %>
                    <%= link_to t('.show', :default => t("helpers.links.show")),
                      task_path(task), :class => 'btn btn-default btn-xs' %>
                    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      task_path(task),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
                </td>
      </tr>
<% end %>
           
    </table>
    </div>
</div>
</
  

    
